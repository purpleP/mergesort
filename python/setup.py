from setuptools import setup

setup(
    name='merge',
    verson='1.0',
    description='Example of Property Based Testing for merge function',
    test_requires=[
        'pytest',
        'hypothesis'
    ]
)
